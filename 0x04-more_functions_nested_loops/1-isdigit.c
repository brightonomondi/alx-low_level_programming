#include "main.h"

/**
 * _isdigit - chacks if a character is a digit
 *
 * @x: the number to be checked
 *
 * Return: 1 for the character that will be a digit or 0 for any else
 */

int _isdigit(int x)
{
	if (x >= 48 && <= 57)
	{
	return (1);
	}
	return (0);
}
