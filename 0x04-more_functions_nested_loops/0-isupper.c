#include "main.h"

/**
* main - check the code.
*  *@x: The number to be checked
* Return: 1 for uppercase letter or 0 for any else.
*/
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
