# include <stdio.h>
/**
 * main - print_alphabets.c
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = 'A'; ch <= 'Z'; ch++)
		putchar(ch);
	for (ch = 'A'; ch <= 'Z'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);

}

